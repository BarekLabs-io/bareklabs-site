node_modules
dist
.env
.env.local
.vercel

# Research toolchain pack (models, internal fiches, delivered report HTML).
# Deliberately untracked: this repository is PUBLIC, and the fiches are working
# papers — they carry unpublished reservations and the method-audit trail.
# Persist them in a private repo if they need to outlive the container.
research-chain/

# sorties de travail de scripts/netdebt.mjs — se regenerent, ne se versionnent pas
dette-nette.txt
dette-nette.log

# memos de travail publies en artifact — pas du contenu de site
memo-*.html
